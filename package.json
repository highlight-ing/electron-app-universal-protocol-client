{
  "name": "@highlight/electron-app-universal-protocol-client",
  "version": "1.3.0",
  "license": "MIT",
  "description": "Unified cross-platform protocol handler for Electron apps, supporting development environments.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "build/Release/*.node",
    "dist",
    "src",
    "binding.gyp",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "node-gyp clean configure rebuild && tsc",
    "clean": "node-gyp clean",
    "example": "electron example/main.js"
  },
  "dependencies": {
    "node-addon-api": "^2.0.0",
    "node-gyp-build": "^4.0.0",
    "typed-emitter": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "electron": "^12.0.0",
    "node-gyp": "^9.0.0",
    "prebuildify-ci": "^1.0.0",
    "prebuildify": "^4.0.0",
    "typescript": "^4.0.0"
  },
  "peerDependencies": {
    "electron": ">12.0.0"
  }
}